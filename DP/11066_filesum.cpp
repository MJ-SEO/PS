#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;

int arr[501];
int dp[501];

int main(){
	ios::sync_with_stdio(0);
	cin.tie(0);
	
	int t;
	cin >> t;

	for(int i=0; i<t; i++){
		int k;
		cin >> k;
		memset(arr, 0, sizeof(arr));
		for(int j=0; j<k; j++){
			cin >> arr[j];
		}
	
			
	}


	return 0;
}

