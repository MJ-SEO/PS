#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int prime(int n){
	int n_prime;
	n_prime+=n;	
	return n_prime;
}

int main(){
	int num = 0;
	do{	
		scanf("%d", &num));
		printf("%d\n", prime(num);
	}while(num!=0);

}
