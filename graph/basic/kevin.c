#include <stdio.h>

typedef graph{
	int vertex;
	int **edge;
}graph_t;

int main(){
	int n, m;
	scanf("%d %d", &n, &m);
	graph_t g[n];
 
	for(int i=0; i<m; i++){
//		g[i].vertex = i+1;		
		scanf("%d %d", &, &);
	}

}
