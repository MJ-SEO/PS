#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;

int line[201];

int main(){
	ios::sync_with_stdio(0);
	cin.tie(0);
	
	int n;
	cin >> n;

	for(int i=0; i<n; i++){
		cin >> line[i];
	}
	return 0;
}

