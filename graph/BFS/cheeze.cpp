#include <iostream>
#include <algorithm>
#include <queue>
#include <vector>

using namespace std;

int N, M;
int map[105][105];
int dist[105][105];

#define X first;
#define Y second;

int main(){
        ios::sync_with_stdio(0);
        cin.tie(0);
	cin >> N >> M;

	for(int i=0; i<n; i++){
		for(int j=0; j<m; j++){
			cin >> map[i][j];
		}
	}

	pair<int, int> a;
	queue<pair<int,int>> que;

	que.push(a);


	while(!que.empty){


	}


        return 0;
}
