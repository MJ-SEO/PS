#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;

int main(){
        ios::sync_with_stdio(0);
        cin.tie(0);
	int arr[10];
	int result = 0;

	for(int i=0; i<10; i++){
		cin >> arr[i];
	}

	cout << result << "\n";
        return 0;
}

