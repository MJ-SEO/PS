#include <iostream>
#include <cstring>
#include <stdlib.h>
#include <deque>
#include <vector>

using namespace std;

int main(){
        int n;
	string buf;
        cin >> n;
	getline(cin, buf);

        deque<int> deck;

        for(int i=0; i<n; i++){
                string str;
                getline(cin, str);
	//      cout << "DEBUG str: " << str << endl;	
		
		
	}
        return 0;
}
