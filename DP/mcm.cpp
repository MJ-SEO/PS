#include <iostream>
#include <algorithm>

using namespace std;

long DP[505][505];

int main(){
	ios::sync_with_stdio(0);	
	cin.tie(0);

	for(int~){
		for(int){
			DP[][] = 0;
			DP[][] = p*q*r;
		}
	}

	for(int~){
		for(int){
			DP[][] = min((DP[][] * DP[][]), (DP[][] * DP[][]));
		}
	}

	
	


	return 0;
}
